[package]
name = "raw-memory-ref"
version = "0.1.0"
edition = "2021"
description = "Rust helper crate for reading raw bytes of memory of data."

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[dev-dependencies]
trybuild = "1.0.55"
